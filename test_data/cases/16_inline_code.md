Use `git status` to check the repository.

Backtick inside code: ``code with ` backtick``.

Multiple `code spans` in `one line` of text.

Long code span: `this is a longer inline code that might wrap on narrow viewports`.
