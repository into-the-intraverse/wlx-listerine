A plain code block:

```
plain text
no syntax highlighting
```

Python code:

```python
def greet(name):
    print(f"Hello, {name}!")

greet("world")
```

JavaScript code:

```javascript
function add(a, b) {
    return a + b;
}
console.log(add(2, 3));
```

Single line code block:

```
just one line
```
